# API base (must match the host your Bearer token was issued for — check JWT "aud" if unsure)
BASE_URL=http://20.207.122.201/evaluation-service

# Either paste a token from a successful /auth response as ACCESS_TOKEN, OR fill the five fields below.
ACCESS_TOKEN=

CLIENT_ID=
CLIENT_SECRET=
EMAIL=
ROLL_NO=RA2311026010091
ACCESS_CODE=

# Optional: local notification API
PORT=3000
# Bearer token clients send to this app (pick any secret string)
API_STUDENT_BEARER=dev-token-change-me
STUDENT_ID=1
